<div class="container">
<h1><%= @workout.time.strftime("%A, %B %d") %></h1>
<h2><%= @workout.name %> with <%= @workout.instructor.name %> at <%= @workout.time.strftime("%I:%M %P") %></h2>
<br><br>
<h2>Signed Up Clients</h2>
  <% @clients.each do |client| %>
  <li><%= client.last_name%>, <%= client.first_name %></li>
  <% end %>
  <br>
  <div class="row">
  <a href="/classes">Return to Classes</a>
</div>
</div>
